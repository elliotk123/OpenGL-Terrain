#version 330 core
layout (location = 0) in vec3 aPos;

out vec2 TexCoord;
out float height;


uniform mat4 view;
uniform mat4 projection;
uniform mat4 model;
void main()
{
	temp = model*vec4(aPos, 1.0);
    gl_Position = projection*view*temp
    TexCoord = vec2(temp.x,temp.y);
	height =(aPos.z);
}